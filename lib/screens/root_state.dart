import 'package:flutter/material.dart';

class RootState {
  static void switchToTab(BuildContext context, int index) {
    // Find the root navigator and switch to the specified tab
    final navigator = Navigator.of(context);
    // We'll need to implement a different approach to access the root state
  }
}